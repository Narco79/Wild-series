{% extends 'base.html.twig' %}

{% block title %}Categories #{{ category.id }}{% endblock %}

{% block body %}

<h1>{{ category.name }}</h1>

{% for program in programs %}

<div>
    <h2>{{ loop.index}} / {{ program.id }} : {{ program.title }}</h2>
    <p>{{ program.synopsis }}</p>
    <a href="{{ path("program_show", { slug: program.slug }) }}">Voir la fiche</a>
</div>
{% else %}
 Aucune série dans cette categorie.

{% endfor %}

{% endblock %}